package nmd.orb.http.servlets;

import nmd.orb.http.BaseServlet;

/**
 * @author : igu
 */
public class BackupServlet extends BaseServlet {

    /*
        response.getOutputHeaders().putSingle("Content-Disposition", format("attachment;filename=\"%s\"", file.getFileName()));
        response.getOutputHeaders().putSingle("Content-Transfer-Encoding", "Binary");
        response.getOutputHeaders().putSingle("Content-Type", file.getFileType());
        response.getOutputHeaders().putSingle("Content-Length", file.getSize());

     */
}
